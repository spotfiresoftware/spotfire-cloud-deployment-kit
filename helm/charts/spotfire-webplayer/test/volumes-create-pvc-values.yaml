nodemanagerConfig:
    serverBackendAddress: "mytss-spotfire-server"

volumes:
  troubleshooting:
    persistentVolumeClaim:
      create: true
      storageClassName: aStorageClass
      resources:
        requests:
          storage: 2Gi
      volumeName: aVolumeName