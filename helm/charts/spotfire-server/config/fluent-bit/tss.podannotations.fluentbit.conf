[FILTER]
    Name    modify
    Alias   tss.podannotations
    Match   tss.*
    Add     pod.name ${POD_NAME}
    Add     pod.namespace ${POD_NAMESPACE}
    Add     container.name ${CONTAINER_NAME}
    Add     pod.id ${POD_ID}
