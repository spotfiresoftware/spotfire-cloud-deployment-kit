global:
  spotfire:
    acceptEUA: true
nodemanagerConfig:
  serverBackendAddress: mytss-spotfire-server
volumes:
  troubleshooting:
    existingClaim: existingclaim1
  customModules:
    existingClaim: existingclaim2
